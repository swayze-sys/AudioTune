<Application x:Class="AudioTune.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:AudioTune.Controls"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Color x:Key="BgColor">#070C12</Color>
        <Color x:Key="PanelColor">#0D1620</Color>
        <Color x:Key="CardColor">#101B27</Color>
        <Color x:Key="CardHoverColor">#142337</Color>
        <Color x:Key="BorderColor">#24384D</Color>
        <Color x:Key="TextColor">#F4F8FF</Color>
        <Color x:Key="MutedColor">#95A7BC</Color>
        <Color x:Key="BlueColor">#148CFF</Color>
        <Color x:Key="CyanColor">#1DD8F4</Color>
        <Color x:Key="GreenColor">#31D88B</Color>
        <Color x:Key="RedColor">#FF6072</Color>
        <Color x:Key="AmberColor">#F2B84B</Color>

        <SolidColorBrush x:Key="BgBrush" Color="{StaticResource BgColor}" />
        <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}" />
        <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}" />
        <SolidColorBrush x:Key="CardHoverBrush" Color="{StaticResource CardHoverColor}" />
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}" />
        <SolidColorBrush x:Key="MutedBrush" Color="{StaticResource MutedColor}" />
        <SolidColorBrush x:Key="BlueBrush" Color="{StaticResource BlueColor}" />
        <SolidColorBrush x:Key="CyanBrush" Color="{StaticResource CyanColor}" />
        <SolidColorBrush x:Key="GreenBrush" Color="{StaticResource GreenColor}" />
        <SolidColorBrush x:Key="RedBrush" Color="{StaticResource RedColor}" />
        <SolidColorBrush x:Key="AmberBrush" Color="{StaticResource AmberColor}" />

        <LinearGradientBrush x:Key="TopBarSurfaceBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#0F1D2A" Offset="0"/>
            <GradientStop Color="#09131E" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="SidebarSurfaceBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#0A1622" Offset="0"/>
            <GradientStop Color="#08111A" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#132230" Offset="0"/>
                        <GradientStop Color="#0D1823" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#69BCED" Offset="0"/>
                        <GradientStop Color="#286897" Offset="0.23"/>
                        <GradientStop Color="#367BAE" Offset="0.72"/>
                        <GradientStop Color="#5BACDD" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#087BD5" BlurRadius="32" ShadowDepth="0" Opacity="0.95"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- The gradient outline adds structure; the zero-offset shadow is the edge glow. -->
        <Style x:Key="LuminousCardStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#69BCED" Offset="0"/>
                        <GradientStop Color="#286897" Offset="0.23"/>
                        <GradientStop Color="#367BAE" Offset="0.72"/>
                        <GradientStop Color="#5BACDD" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#087BD5" BlurRadius="32" ShadowDepth="0" Opacity="0.95"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GreenLuminousCardStyle" TargetType="Border" BasedOn="{StaticResource LuminousCardStyle}">
            <Setter Property="BorderBrush" Value="#2F9D82"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#0CA785" BlurRadius="32" ShadowDepth="0" Opacity="0.94"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PurpleLuminousCardStyle" TargetType="Border" BasedOn="{StaticResource LuminousCardStyle}">
            <Setter Property="BorderBrush" Value="#6956A8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#6742C5" BlurRadius="30" ShadowDepth="0" Opacity="0.92"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AmberLuminousCardStyle" TargetType="Border" BasedOn="{StaticResource LuminousCardStyle}">
            <Setter Property="BorderBrush" Value="#90653D"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#B57028" BlurRadius="30" ShadowDepth="0" Opacity="0.90"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>

        <Style x:Key="MutedTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="FlatButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#142233" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1A3048" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#365C7E" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.78" /></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Bd" Property="Opacity" Value="0.38" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource FlatButtonStyle}">
            <Setter Property="Background" Value="{StaticResource BlueBrush}" />
            <Setter Property="BorderBrush" Value="#299DFF" />
            <Setter Property="Padding" Value="18,11" />
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#B7C7DA" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="Margin" Value="10,3" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter />
                            </Border>
                            <Border x:Name="ActiveRail" Width="3" Height="28" CornerRadius="0,2,2,0" Background="{StaticResource CyanBrush}" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="#11283F" /><Setter TargetName="Bd" Property="BorderBrush" Value="#284B68" /></Trigger>
                            <Trigger Property="Tag" Value="Selected"><Setter TargetName="Bd" Property="Background" Value="#173F70" /><Setter TargetName="Bd" Property="BorderBrush" Value="#2C6594" /><Setter TargetName="ActiveRail" Property="Visibility" Value="Visible" /><Setter Property="Foreground" Value="White" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#101B27" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="5" Margin="2">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="ItemBorder" Property="Background" Value="#173754" /></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter TargetName="ItemBorder" Property="Background" Value="#154779" /></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#132131" />
            <Setter Property="Foreground" Value="#F4F8FF" />
            <Setter Property="BorderBrush" Value="#2D4962" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="11,0,8,0" />
            <Setter Property="MinHeight" Value="36" />
            <Setter Property="MaxDropDownHeight" Value="360" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboItemStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid SnapsToDevicePixels="True">
                            <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ComboBorder" Background="#132131" BorderBrush="#2D4962" BorderThickness="1" CornerRadius="7">
                                            <Grid>
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="34"/></Grid.ColumnDefinitions>
                                                <ContentPresenter Margin="11,0,5,0" VerticalAlignment="Center" HorizontalAlignment="Left"
                                                                  Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  ContentStringFormat="{Binding SelectionBoxItemStringFormat, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  TextElement.Foreground="#F4F8FF" />
                                                <Border Grid.Column="1" BorderBrush="#274055" BorderThickness="1,0,0,0">
                                                    <controls:LineIcon Kind="ChevronDown" Width="14" Height="14" Stroke="#AFC4D9" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ComboBorder" Property="BorderBrush" Value="#4274A0"/><Setter TargetName="ComboBorder" Property="Background" Value="#17283A"/></Trigger>
                                            <Trigger Property="IsChecked" Value="True"><Setter TargetName="ComboBorder" Property="BorderBrush" Value="#148CFF"/></Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                <Border Background="#0D1722" BorderBrush="#31516D" BorderThickness="1" CornerRadius="8" Margin="0,4,0,0" Padding="4" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                    <ScrollViewer SnapsToDevicePixels="True" VerticalScrollBarVisibility="Auto" MaxHeight="360">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AbModeButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#0D1824" />
            <Setter Property="BorderBrush" Value="#2A4157" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,20" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinHeight" Value="120" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="#13283C"/><Setter TargetName="Bd" Property="BorderBrush" Value="#3D6B91"/></Trigger>
                            <Trigger Property="Tag" Value="Selected"><Setter TargetName="Bd" Property="Background" Value="#103C66"/><Setter TargetName="Bd" Property="BorderBrush" Value="#1B9CFF"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Bd" Property="Opacity" Value="0.38"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SwitchStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="44"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <Border x:Name="Track" Width="38" Height="20" CornerRadius="10" Background="#27384A" VerticalAlignment="Center">
                                <Ellipse x:Name="Thumb" Width="16" Height="16" Fill="#C7D2DE" HorizontalAlignment="Left" Margin="2" />
                            </Border>
                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{StaticResource BlueBrush}" />
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="Thumb" Property="Fill" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
